package com.example.GestionUser.repositories;

import com.example.GestionUser.entities.DimCarte;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DimCarteRepository extends JpaRepository<DimCarte, Long> {
}